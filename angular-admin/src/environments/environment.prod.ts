export const environment = {
  production: true,
  apiUrl: 'https://your-production-api-url.com/api',
  pusher: {
    key: 'your-production-pusher-key',
    cluster: 'your-production-pusher-cluster',
  }
};
